export default function HomeIcon({ size }: { size: number }) {
  return (
    <div
      style={{
        display: "block",
        width: size + "px",
        height: size + "px",
        background: "Red",
      }}
    ></div>
  );
}
